import React from 'react';
import AppNavigator from './AppNavigator'; // Assuming AppNavigator.js is in the same directory

const App: React.FC = () => {
  return <AppNavigator />;
};

export default App;
